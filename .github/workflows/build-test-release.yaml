name: Build, test and release
on: 
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    env:
      # Environment variables
      BUILD_DIR: build

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Non-shallow clone - fetch all history for all tags and branches - may be of use later in pipeline.
      - name: Install and setup dependencies
        run: |
          sudo apt-get update
          sudo bash . scripts/setup_for_linux.sh
      - name: Compile
        run: |
          sudo bash scripts/build_for_linux.sh
      #- name: Save/upload package
        #uses: actions/upload-artifact@v4
        #with:
          #name: ${{ github.repository }}
          #path: |
            #${{ env.BUILD_DIR }}/* # All folders inside the build folder.
